# do not modify these
root: ./
name: simulation
socket_name: mrs
attach: false
tmux_options: -f /etc/ctu-mrs/tmux.conf
# you can modify these
startup_window: activation
# pre_window: export RUN_TYPE=simulation; export UAV_TYPE=x500; export BOND=false; export CUSTOM_CONFIG=./config/custom_config.yaml; export WORLD_CONFIG=./config/world_config.yaml; export NETWORK_CONFIG=./config/network_config.yaml; export PLATFORM_CONFIG=`rospack find mrs_multirotor_simulator`/config/mrs_uav_system/$UAV_TYPE.yaml

pre_window:
  - export UAV_NAME=uav1
  - export USE_SIM_TIME="true"
  - export RUN_TYPE=simulation
  - export UAV_TYPE=x500
  - export CUSTOM_CONFIG=./config/custom_config.yaml
  - export WORLD_CONFIG=./config/world_config.yaml
  - export WORLD_FILE=`ros2 pkg prefix mrs_gazebo_common_resources`/share/mrs_gazebo_common_resources/worlds/forest.sdf
  # - export RMW_IMPLEMENTATION=rmw_zenoh_cpp
  - export PLATFORM_CONFIG=`ros2 pkg prefix mrs_uav_gazebo_simulator`/share/mrs_uav_gazebo_simulator/config/mrs_uav_system/$UAV_TYPE.yaml
  - export MAP_LENGTH=28.0; export MAP_WIDTH=25.0; export MAP_HEIGHT=10.0; export MAP_OBS_NUM=20; export MAP_CIRCLE_NUM=0

windows:
  - router:
      layout: tiled
      panes:
        - ros2 run rmw_zenoh_cpp rmw_zenohd
  - core:
      layout: tiled
      panes:
        - ros2 launch ./core_without_status.launch.py
          platform_config:=$PLATFORM_CONFIG
          custom_config:=./config/custom_config.yaml
          world_config:=./config/world_config.yaml
          network_config:=./config/network_config.yaml
  - status:
      layout: tiled
      panes:
        - export UAV_NAME=uav1; waitForRos; ros2 run mrs_uav_status status.sh
  - status_proxy:
      layout: tiled
      panes:
        - export UAV_NAME=uav1; export UAV_TYPE=x500; waitForRos; python3 ./status_proxy.py
  - activation:
      layout: tiled
      synchronize: after
      panes:
        - export UAV_NAME=uav1; waitForOffboard; history -s ros2 service call /uav1/rbl_controller/activation std_srvs/srv/Trigger
  - rbl_controller:
      layout: tiled
      panes:
        - export UAV_NAME=uav1; waitForCore; ros2 launch rbl_controller_node rbl_controller.launch.py custom_config:=./config/rbl_controller.yaml pcl_topic:=/uav2/losos_server/current_submap_pc
  - initial_goal:
      layout: tiled
      panes:
        - >-
          export UAV_NAME=uav1; waitForRos; until ros2 service list | grep -q "^/uav1/rbl_controller/goto$"; do sleep 1; done; ros2 service call /uav1/rbl_controller/goto mrs_msgs/srv/Vec4 "{goal: [-20.0, 0.0, 1.5, 0.0]}"
  - goto:
      layout: tiled
      synchronize: after
      panes:
        - 'history -s ros2 service call /uav1/rbl_controller/goto mrs_msgs/srv/Vec4 \"{goal: [20.0, 0.0, 2.0, 0.0]}\"'
  - filter_reflective_uavs:
      layout: tiled
      synchronize: after
      panes:
        - export UAV_NAME=uav1; ros2 launch filter_reflective_uavs filter_reflective_uavs.launch.py uav_name:=$UAV_NAME global_frame:=$UAV_NAME/world_origin

  - simulator:
      layout: tiled
      panes:
        - ros2 launch mrs_multirotor_simulator multirotor_simulator.launch.py custom_config:=./config/simulator.yaml

  - takeoff:
      layout: tiled
      panes:
        - ros2 launch mrs_uav_autostart automatic_start.launch.py custom_config:=./config/automatic_start.yaml
        - waitForCore && ./takeoff.sh
  - hw_api:
      layout: tiled
      panes:
        - ros2 launch mrs_multirotor_simulator hw_api.launch.py
  # - hw_api:
  #     layout: tiled
  #     panes:
  #       - export UAV_NAME=uav1; ros2 launch mrs_multirotor_simulator hw_api.launch custom_config:=./config/hw_api.yaml nodelet_manager_name:=$UAV_NAME\_nodelet_manager

  - automatic_start:
      layout: tiled
      panes:
        - export UAV_NAME=uav1; ros2 launch mrs_uav_autostart automatic_start.launch.py custom_config:=./config/automatic_start.yaml

  # - tf_connector:
  #     layout: tiled
  #     panes:
  #       - waitForTime; roslaunch mrs_tf_connector tf_connector.launch custom_config:=./config/tf_connector.yaml
  #
  - rviz:
      layout: tiled
      panes:
        - rviz2 -d forest.rviz

  # - layout:
  #     layout: tiled
  #     panes:
  #       - waitForControl; sleep 5; ~/.i3/layout_manager.sh ./layout.json
  #

  # - map_plan:
  #     layout: tiled
  #     panes:
  #       - ros2 launch mrs_octomap_mapping_planning mapplan.launch.py custom_config:=./config/mapplan_config.yaml lidar_3d_0:=filter_reflective_uavs/filtered_pcl custom_config:=./config/mapplan_config.yaml
  # - rosbag:
  #     layout: tiled
  #     panes:
  #       - export UAV_NAME=uav1; waitForControl; sleep 5; ./record_rbl.sh ${SIM_ID}

  # - goal_monitoring:
  #     layout: tiled
  #     panes:
  #       - export UAV_NAME=uav1; waitForControl; sleep 5; rosrun rbl_controller monitor_goal.py _goals_file:=/home/manuel/nmpc_workspace/src/rbl_controller/tmux/circle10/config/rbl_controller.yaml
  # - goto_end:
  #     layout: tiled
  #     synchronize: after
  #     panes:
  #       - "export UAV_NAME=uav1; waitForTakeoff; sleep 15; rosservice call /uav1/rbl_controller/goto 'goal: [-15.00, 0.00, 5.00, 0.0]'"
  #       - "export UAV_NAME=uav2; waitForTakeoff; sleep 15; rosservice call /uav2/rbl_controller/goto 'goal: [-12.14, -8.82, 5.00, 0.0]'"
  #       - "export UAV_NAME=uav3; waitForTakeoff; sleep 15; rosservice call /uav3/rbl_controller/goto 'goal: [-4.64, -14.27, 5.00, 0.0]'"
  #       - "export UAV_NAME=uav4; waitForTakeoff; sleep 15; rosservice call /uav4/rbl_controller/goto 'goal: [4.64, -14.27, 5.00, 0.0]'"
  #       - "export UAV_NAME=uav5; waitForTakeoff; sleep 15; rosservice call /uav5/rbl_controller/goto 'goal: [12.14, -8.82, 5.00, 0.0]'"
  # - activation_auto:
  #     layout: tiled
  #     synchronize: after
  #     panes:
  #       - export UAV_NAME=uav1; sleep 60;  rosservice call /$UAV_NAME/rbl_controller/activate
  #       - export UAV_NAME=uav2; sleep 60;  rosservice call /$UAV_NAME/rbl_controller/activate
  #       - export UAV_NAME=uav3; sleep 60;  rosservice call /$UAV_NAME/rbl_controller/activate
  #       - export UAV_NAME=uav4; sleep 60;  rosservice call /$UAV_NAME/rbl_controller/activate
  #       - export UAV_NAME=uav5; sleep 60;  rosservice call /$UAV_NAME/rbl_controller/activate
  #       - export UAV_NAME=uav6; sleep 60;  rosservice call /$UAV_NAME/rbl_controller/activate
  #       - export UAV_NAME=uav7; sleep 60;  rosservice call /$UAV_NAME/rbl_controller/activate
  #       - export UAV_NAME=uav8; sleep 60;  rosservice call /$UAV_NAME/rbl_controller/activate
  #       - export UAV_NAME=uav9; sleep 60;  rosservice call /$UAV_NAME/rbl_controller/activate
  #       - export UAV_NAME=uav10; sleep 60;  rosservice call /$UAV_NAME/rbl_controller/activate

  - map_generator:
      layout: tiled
      panes:
        # - ros2 run map_generator random_forest
        # - export UAV_NAME=uav1; waitForRos; ros2 launch map_generator map_generator.launch map_frame:=map map_size_x:=$MAP_LENGTH map_size_y:=$MAP_WIDTH map_size_z:=$MAP_HEIGHT map_obs_num:=$MAP_OBS_NUM map_circle_num:=$MAP_CIRCLE_NUM
        # - ros2 bag play -l super_dense_ros2/super_dense_ros2.mcap
        # - ros2 bag play -l forest/forest.mcap
        - ./download_and_play_bag.sh
        # - ros2 bag play -l circle_ros2/circle_ros2.mcap
        - ./map_tf.sh
